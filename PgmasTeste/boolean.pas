program bool1(input, output);
  var   a, b : boolean;
        c, d : integer;
  
  procedure p(d : boolean);
    begin
      d:=d and false
     end;
   begin
    a:=true;
    b:=false;
    p(true);
    p(c>d);
    p(a)
  end.
